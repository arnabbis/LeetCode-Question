String.prototype.checkName = (name)=>{
    let data;
    if(name=="Arnab"){
        data = name.split('');
    }else{
        data = name.slice(
    if
}

let name = "Arnab";

console.log(name.checkName(name))
